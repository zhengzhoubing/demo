<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bin.demo.Mapper.MyMapper">
    <select id="qryTemplateType" resultType="templateType">
        SELECT t.template_type_id,
               t.up_template_type_id,
               t.type_name,
               t.type_desc,
               t.priority,
               create_date,
               create_staff,
               update_date,
               update_staff
          FROM template_type_t t
         WHERE 1=1
           AND t.template_type_id = #{templateTypeId}
    </select>
</mapper>